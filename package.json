{
  "name": "parcel",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel src/index.html --out-dir=tmp --open",
    "start:prod": "yarn build && parcel dist/index.html --out-dir=tmp --open",
    "build": "rm -rf dist && parcel build src/index.html --out-dir=dist --public-url='./'"
  },
  "keywords": [],
  "author": "",
  "dependencies": {
    "babel-polyfill": "^6.26.0"
  },
  "devDependencies": {
    "autoprefixer": "^9.7.1",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "parcel-bundler": "^1.12.4",
    "postcss-modules": "^1.4.1"
  }
}